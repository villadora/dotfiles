case ${TERM} in
    xterm*|rxvt*|Eterm|aterm|kterm|gnome*|interix)
        PROMPT_COMMAND='echo -ne "\033]0;${USER}@${HOSTNAME%%.*}:${PWD/$HOME/~}\007"'
        ;;
    screen)
        PROMPT_COMMAND='echo -ne "\033_${USER}@${HOSTNAME%%.*}:${PWD/$HOME/~}\033\\"'
        ;;
esac


RESET="${HOME}/.reset"
touch "${RESET}"
echo "PATH=${PATH}" > "${RESET}"

# add commands to PATH
PATH=$PATH:$HOME/.dotfiles/bin:$HOME/bin


# add common profile configs
source ~/.dotfiles/profile_common.sh

# Functions
man() { command man "$@" | less; }

# add git completion support
source ~/.dotfiles/bash/git-completion.sh
source ~/.dotfiles/bash/git-prompt.sh
source ~/.dotfiles/zsh/hub-zsh_completion.sh

if [ -f ~/.bashrc ]; then
   source ~/.bashrc
fi

if [ -f ~/.bash_custom ]; then
    source ~/.bash_custom
fi
export PATH="/Users/villadora/dev/sofa:$PATH"
source /Users/villadora/dev/sofa/resources/completion/bash_autocomplete
