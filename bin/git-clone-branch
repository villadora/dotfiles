#!/bin/bash

REMOTE_REPO=$1
BRANCH=$2

if [[ -z "$3" ]];then
    DIR=`expr "$REPO" : '.*\/\(.*\).git$'`
    echo $DIR
else
    DIR=$3
fi


mkdir $DIR
cd $DIR
git init
git remote add -t $BRANCH -f origin $REMOTE_REPO
git checkout $BRANCH


